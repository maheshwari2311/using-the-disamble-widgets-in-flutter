import 'package:flutter/material.dart';

class MySing extends StatelessWidget {
  const MySing({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(
          "signup page",
        ),
      ),
    );
  }
}
